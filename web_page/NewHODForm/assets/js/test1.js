function hi(name){
    alert('Welcome ' + name);  
}



/*$data.application.nameWithInitials = "mursina"
var r = {
	    "number": 5
	};

//if( $data.application && $data.application_Courses ){
	//attach lazy references variables to parent variables
    console.log("Hi");
	$data.application.Courses = $data.application_Courses;
	$data.application.nameWithInitials = $data.batchMap.regNumId.name;

	
	console.log($data.application);
	var m = {
		//map application variable to expected task contract input
		applicationInput: {
			Courses: $data.application.Courses.map( it => ({
				persistenceId_string: it.persistenceId_string !== undefined ? it.persistenceId_string : null
			}))
		}
	}
	
	r = {
	    "number": 6
	};
	console.log(r);
	console.log(m);
	return r;
//}
*/
